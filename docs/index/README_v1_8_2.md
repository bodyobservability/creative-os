# HVLIEN v1.8.2 — Index Precision Bundle

Includes:
- Updated expected artifact parsing for racks (manifest expansion)
- Stale detection in ArtifactIndexBuilder

Drop-in patch for the v1.8 indexer.

Wiring:
- No new CLI commands; still uses `hvlien index build` and `hvlien index status`.
