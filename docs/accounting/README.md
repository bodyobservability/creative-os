# Creative-OS Accounting Bundle (v1.1+ complete)

This bundle contains:
- Specs (Bundles 01–03) under `shared/specs/accounting/`
- QuickStart at `docs/accounting/CREATIVE_OS_ACCOUNTING_QUICKSTART.md`
- Scripts under `accounting/scripts/` (exports + auto-classifier table)
- Local-only archives under `accounting/epochs/` (do not commit zips)

Use the QuickStart to run end-to-end.


## Git hygiene & backups
See `docs/accounting/GITIGNORE_SNIPPET_ACCOUNTING.txt` and `docs/accounting/BACKUP_SETUP.md`.
