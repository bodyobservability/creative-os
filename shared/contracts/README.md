# Shared Contracts

`shared/contracts/` contains cross-domain contracts that define truth across operator personas
(materials, manufacturing interfaces, evidence schemas, persona manifests).

These files contain **no execution logic**. They are validated and enforced by the kernel.
