name: swift-tests

on:
  push:
    branches: ["**"]
    paths:
      - "tools/automation/swift-cli/**"
      - "**/*.swift"
      - "**/Package.swift"
      - "**/Package.resolved"
  pull_request:
    paths:
      - "tools/automation/swift-cli/**"
      - "**/*.swift"
      - "**/Package.swift"
      - "**/Package.resolved"

concurrency:
  group: swift-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  swift-tests:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache SwiftPM + build
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/org.swift.swiftpm
            tools/automation/swift-cli/.build
          key: swiftpm-${{ runner.os }}-${{ hashFiles('tools/automation/swift-cli/Package.resolved') }}
          restore-keys: |
            swiftpm-${{ runner.os }}-

      - name: Swift build
        working-directory: tools/automation/swift-cli
        run: swift build -c release

      - name: Swift test
        working-directory: tools/automation/swift-cli
        run: swift test
