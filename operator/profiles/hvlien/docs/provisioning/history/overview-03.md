# HVLIEN legacy — Performance Set Export

Adds:
- Spec + receipt schema for performance set export
- CLI subcommand: `wub assets export-performance-set`
- Save-As apply plan generator using the same save-dialog primitive as legacy

Wiring:
- Add ExportPerformanceSet as a subcommand under Assets.
