# Accounting Operator - Packs

This directory anchors packs for Accounting Operator.

Current defaults pack:
- `operator/packs/accounting-defaults/`

This directory exists to keep persona asset roots stable and to prevent pack sprawl.
Packs are non-authoritative bundles; the kernel remains the source of execution semantics.
